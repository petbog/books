import React from 'react';
import s from './Greeter.module.scss';
import { FC } from 'react';

const Greeter:FC = () => {
    return (
        <div className={s.Greeter}>
            
        </div>
    );
};

export default Greeter;